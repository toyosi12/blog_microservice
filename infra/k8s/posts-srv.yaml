apiVersion: v1
kind: Service
metadata: 
  name: posts-srv
spec: 
  type: NodePort
  selector:
    app: posts
  ports: 
    - name: posts
      protocol: TCP
      port: 4000 # port from the pod 
      targetPort: 4000 # exposed port from the application in container
